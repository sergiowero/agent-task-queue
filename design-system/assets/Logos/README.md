# Logos

- `agentq-mark.svg`: the app mark. A 32×32 tile with radius 9 and a linear gradient from indigo `#6366f1` (top left) to violet `#a855f7` (bottom right), carrying three white, round-capped queue lines 14, 10 and 6 units long. It is full color; never recolor it. The sidebar shows it at 36px beside "AgentQ" in `brand-name`, with a 35% `#6366f1` drop shadow.
- `agentq-favicon.svg`: the browser-tab icon. The same lines on a radius-8 tile, with the gradient ending at `#8b5cf6`.
- There is no wordmark file. Set the name in Inter bold next to the mark.
